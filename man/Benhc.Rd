\name{Benhc}
\alias{Benhc}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{Performs bootstrap ensemble hierarchical clustering for categorical data.
}
\description{
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
}
\usage{
Benhc(x, En)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{x}{A nxp data matrix or data frame; n is the number of observations and p is the number of dimensions.}
%%     ~~Describe \code{x} here~~
  \item{En}{Number of ensembles.
%%     ~~Describe \code{B} here~~
}
}
\details{
The function 'Benhc' generates a dissimilarity via the bootstrap ensemble. The ensemble disssimilarities are generated according to the procedure given in Amiri et al. (2015). This function includes one more step than "enhc", as in each ensemble a resampling of the data is done. 
}
\value{
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
}
\references{Amiri, S., Clarke, B. & Clarke J. (2015). Clustering categorical data via ensembling dissimilarity matrices.  arXiv:1506.07930.
}
\author{Saeid Amiri, Bertrand Clarke and Jennifer Clarke
%%  ~~who you are~~
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
 \code{\link{enhc}}
}
\examples{
data('zoo')
### zoo includes the zoo data downloaded from UCI
### MAchine Learning Repository
### Calculate ensemble dissimilarities with 150 ensembles
disten<-Benhc(zoo$obs,En=150)
### This function performs a hierarchical cluster analysis using 
### dissimilarities obtained by the ensembling
en<-hclust(disten,method='average')
### The plot of dendrogram can be generated by
plot(en,label=zoo$lab)
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
%\keyword{ ~kwd1 }
%\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
\keyword{lowdimension}